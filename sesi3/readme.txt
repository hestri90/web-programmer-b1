sesi 3 belajar

1. css
2. instalasi github
    cara menyambungkan github ke vscode
    1. menu setting config token new(untuk membuat token)
    2. install git
    3. membuat repositori/penyimpanan cloud
    4. clone repositori ke dalam local/komputer saya
    5. memasukan data coding ke dalam repositori/git push

    clone repositori ke dalam local jika mau mengambil data
    dari git atau memindahkan ke local

    git clone alamat github

    git config --global user.email "email"
    git config --global user.name "username"

    menambahkan data ke dalam repository 
    git add .
    git commit -m "komentar"
    git push